# ==============================================================
# Environment Variables Template
# Copy this file to .env and fill in your values.
# Application variables are validated by src/env.js. The final tooling and
# infrastructure section lists the few intentional exceptions.
# ==============================================================


# ==============================================================
# App
# ==============================================================

# Application environment: dev | staging | prod
NEXT_PUBLIC_APP_ENV=dev

# Product branding used by emails and default support links.
NEXT_PUBLIC_APP_NAME=SaaS App
NEXT_PUBLIC_SUPPORT_EMAIL=support@yourdomain.com

# Password login is disabled by default. To opt in, list exact email addresses.
# This list is public so the login UI can show a password field for those accounts.
# Keep it empty unless you intentionally provision password-login users.
NEXT_PUBLIC_PASSWORD_LOGIN_EMAILS=
# Development-only seed password (minimum 12 characters). Never committed or logged.
# PASSWORD_LOGIN_SEED_PASSWORD=

# Public URL of the app (payment callbacks, webhooks, referral links, etc.)
# Falls back to AUTH_URL (or legacy NEXTAUTH_URL) if not set.
APP_URL=http://localhost:3000

# Runtime services to start in standalone mode.
# Default is Web + Worker. Set SERVICE_MODE=all or SERVICE_MODE=api to enable the optional Hono API service.
# Options: web,worker | all | web | api | worker | web,api | web,api,worker
SERVICE_MODE=web,worker

# Runtime listeners. Defaults: Web 3000, Worker 3001, optional API 3002.
PORT=3000
WEB_HOST=0.0.0.0
WORKER_PORT=3001
API_PORT=3002

# Optional module modes: off | auto | on.
# auto enables a module when its required config and dependencies are present.
# on fails fast when required config or dependencies are missing.
# TOUCH_MODE=auto
# SUPPORT_AUTOMATION_MODE=auto
# AI_CHAT_MODE=auto
# VELOBASE_GATEWAY_MODE=auto
# IMAGE_GENERATION_MODE=auto
# AFFILIATE_MODE=auto
# STRIPE_MODE=auto
# LEMONSQUEEZY_MODE=auto
# NOWPAYMENTS_MODE=auto
# PAYMENT_RECONCILIATION_MODE=auto
# GOOGLE_ADS_MODE=auto
# CONVERSION_ALERT_MODE=auto
# LARK_MODE=auto
# TELEGRAM_MODE=auto
# POSTHOG_MODE=auto

# ==============================================================
# Authentication (Auth.js v5 via next-auth 5 beta)
# Generate a secret with: npx auth secret
# The first non-empty AUTH_* value takes precedence. An empty AUTH_* value falls
# back to its NEXTAUTH_* alias so existing deployments remain compatible.
# ==============================================================

AUTH_SECRET=your-auth-secret-here
AUTH_URL=http://localhost:3000
# NEXTAUTH_SECRET=your-auth-secret-here
# NEXTAUTH_URL=http://localhost:3000

# Google OAuth
# Create credentials at: https://console.cloud.google.com/apis/credentials
AUTH_GOOGLE_ID=
AUTH_GOOGLE_SECRET=

# GitHub OAuth
# Create an OAuth App at: https://github.com/settings/developers
# Callback URL: https://yourdomain.com/api/auth/callback/github
AUTH_GITHUB_ID=
AUTH_GITHUB_SECRET=

# Separate GitHub OAuth app used to connect repositories in AI Chat.
GITHUB_CLIENT_ID=
GITHUB_CLIENT_SECRET=

# Discord OAuth (optional)
AUTH_DISCORD_ID=
AUTH_DISCORD_SECRET=


# ==============================================================
# Database (PostgreSQL via Prisma)
#
# Option A - Docker (local dev):
#   docker compose up -d
#   Docker default: postgresql://velobase:velobase@localhost:5432/velobase
#
# Option B - Cloud:
#   Neon:     postgresql://user:pass@ep-xxx.neon.tech/neondb?sslmode=require
#   Supabase: postgresql://postgres.xxx:pass@pooler.supabase.com:6543/postgres
#   RDS:      postgresql://user:pass@your-instance.rds.amazonaws.com:5432/dbname
# ==============================================================

DATABASE_URL=postgresql://velobase:velobase@localhost:5432/velobase


# ==============================================================
# Redis (BullMQ job queues + rate limiting)
#
# Option A - Single URL (Velobase Cloud / managed Redis):
#   Managed URL example: redis://:token@redis-host:6379
#
# Option B - Individual fields (local dev / self-hosted):
#   docker compose up -d
#
# When REDIS_URL is set, REDIS_HOST/PORT/PASSWORD are ignored.
# ==============================================================

# REDIS_URL=
REDIS_HOST=127.0.0.1
REDIS_PORT=6379
REDIS_USER=
REDIS_PASSWORD=
REDIS_DB=0

# ==============================================================
# Object Storage
# STORAGE_PROVIDER options: r2 | filesystem | aws | aliyun | gcs | minio
# Default is r2. Server-side writes fall back to filesystem when object storage
# is not configured or upload fails.
# ==============================================================

STORAGE_PROVIDER=r2

# Cloudflare R2 example:
STORAGE_ENDPOINT=https://<account-id>.r2.cloudflarestorage.com
STORAGE_BUCKET=your-bucket-name
STORAGE_ACCESS_KEY_ID=your-access-key-id
STORAGE_SECRET_ACCESS_KEY=your-secret-access-key

# Filesystem fallback. Relative paths are resolved from the project root.
STORAGE_FILESYSTEM_ROOT=public/storage
# Optional absolute URL base for filesystem files. If unset, generated URLs are
# root-relative, e.g. /storage/a.png.
# STORAGE_FILESYSTEM_PUBLIC_BASE_URL=http://localhost:3000

# AWS S3 / Aliyun OSS / GCS also require:
# STORAGE_REGION=us-east-1

# (Optional) Path prefix for multi-tenant shared bucket (Velobase Cloud sets this automatically)
# STORAGE_PATH_PREFIX=

# CDN base URL for serving stored files
CDN_BASE_URL=https://cdn.example.com


# ==============================================================
# AI Providers
# At least one LLM key is required for chat/generation features.
# ==============================================================

# Anthropic Claude
# https://console.anthropic.com/
ANTHROPIC_API_KEY=

# OpenAI
# https://platform.openai.com/api-keys
OPENAI_API_KEY=
# Optional: custom base URL for OpenAI-compatible proxies
# OPENAI_BASE_URL=

# OpenRouter (routes to multiple LLM providers)
# https://openrouter.ai/keys
OPENROUTER_API_KEY=

# xAI (Grok)
# https://console.x.ai/
XAI_API_KEY=

# Datalab (document parsing / OCR)
# https://www.datalab.to/
DATALAB_API_KEY=
# DATALAB_BASE_URL=https://api.datalab.to

# WaveSpeedAI (image generation provider)
WAVESPEED_API_KEY=
# WAVESPEED_BASE_URL=https://api.wavespeed.ai
# WAVESPEED_REQUEST_TIMEOUT_MS=30000

# ModelRunner (image generation provider)
MODELRUNNER_KEY=
# MODELRUNNER_BASE_URL=https://modelrunner.run
# MODELRUNNER_QUEUE_URL=https://queue.modelrunner.run
# MODELRUNNER_REQUEST_TIMEOUT_MS=30000

# Velobase Gateway (OpenAI-compatible model gateway)
# Reuses VELOBASE_API_KEY by default. Set this only when the gateway should use
# a different Velobase project key or a customer-scoped key.
# VELOBASE_GATEWAY_API_KEY=vb_live_or_vb_customer_key
# VELOBASE_GATEWAY_BASE_URL=https://api.velobase.io/v1
# VELOBASE_GATEWAY_DEFAULT_MODEL=deepseek/deepseek-v4-pro
# VELOBASE_GATEWAY_TEST_CUSTOMER_ID=user_123
# VELOBASE_GATEWAY_REQUEST_TIMEOUT_MS=30000


# ==============================================================
# Payments
# ==============================================================

# ---------- Stripe (primary) ----------
# https://dashboard.stripe.com/apikeys
STRIPE_SECRET_KEY=sk_test_...
NEXT_PUBLIC_STRIPE_PUBLISHABLE_KEY=pk_test_...
# Webhook secret:
#   Production: from https://dashboard.stripe.com/webhooks
#   Local quick checkout tests: any non-empty placeholder is enough for provider registration
#   Local webhook testing: run `make stripe` and copy the whsec_xxx from the output
STRIPE_WEBHOOK_SECRET=whsec_...

# ---------- LemonSqueezy (optional payment provider) ----------
# Configure these values and set LEMONSQUEEZY_MODE=auto/on to enable checkout tests.
LEMONSQUEEZY_API_KEY=
LEMONSQUEEZY_STORE_ID=
LEMONSQUEEZY_WEBHOOK_SECRET=
# Local dashboard checkout tests can use fallback variant IDs when product metadata
# does not include lemonsqueezyVariantId / lemonsqueezy.variantId.
LEMONSQUEEZY_TEST_MODE=true
LEMONSQUEEZY_TEST_VARIANT_ID=
LEMONSQUEEZY_TEST_SUBSCRIPTION_VARIANT_ID=

# ---------- NowPayments (crypto, optional) ----------
# https://nowpayments.io/
NOWPAYMENTS_API_KEY=
NOWPAYMENTS_IPN_SECRET=
# Default crypto currency for payments (default: usdttrc20)
# NOWPAYMENTS_PAY_CURRENCY=usdttrc20

# ---------- Force payment gateway (testing only) ----------
# Override default gateway routing. Options: STRIPE | NOWPAYMENTS | LEMONSQUEEZY
# FORCE_PAYMENT_GATEWAY=

# Allow the reconciliation processor to run outside production for local tests.
# PAYMENT_RECONCILIATION_FORCE=1


# ==============================================================
# Telegram Bot (Telegram Stars payment + notifications)
# Create a bot via: https://t.me/BotFather
# ==============================================================

TELEGRAM_BOT_TOKEN=
TELEGRAM_WEBHOOK_SECRET=your-custom-webhook-secret
NEXT_PUBLIC_TELEGRAM_BOT_USERNAME=your_bot_username

# Optional one-off override used by the Telegram webhook setup script.
# WEBHOOK_URL=https://yourdomain.com/api/webhooks/telegram


# ==============================================================
# Email Services
# EMAIL_PROVIDER: comma-separated provider chain (order = priority)
#   "resend"              → Resend only
#   "sendgrid"            → SendGrid only
#   "resend,sendgrid"     → Resend first, SendGrid fallback (default)
#   "sendgrid,resend"     → SendGrid first, Resend fallback
# ==============================================================

EMAIL_PROVIDER=resend,sendgrid

# Required for production email delivery, e.g. SaaS App <noreply@yourdomain.com>
EMAIL_FROM=

# Resend: https://resend.com/api-keys
RESEND_API_KEY=
RESEND_WEBHOOK_SECRET=whsec_...

# SendGrid: https://app.sendgrid.com/settings/api_keys
SENDGRID_API_KEY=

# Support Automation mailbox.
# Auto support automation requires these values plus OPENROUTER_API_KEY.
SUPPORT_EMAIL_ADDRESS=
SUPPORT_EMAIL_PASSWORD=
SUPPORT_IMAP_HOST=
# SUPPORT_IMAP_PORT=993
SUPPORT_SMTP_HOST=
# SUPPORT_SMTP_PORT=465
# SUPPORT_EMAIL_FROM="SaaS App Support <support@yourdomain.com>"


# ==============================================================
# Lark / Feishu Bot (internal notifications)
# Lark: https://open.larksuite.com/
# Feishu (China): https://open.feishu.cn/
# Set LARK_USE_FEISHU=true to use Feishu endpoints instead of Lark.
# ==============================================================

LARK_APP_ID=
LARK_APP_SECRET=
LARK_USE_FEISHU=false
# Optional: default chat/group ID for notifications
# LARK_DEFAULT_CHAT_ID=
# Optional user open ID to @mention in billing reconciliation alerts.
# LARK_BILLING_RECONCILIATION_AT_OPEN_ID=

# Required if you configure Lark event subscriptions with encryption
LARK_ENCRYPT_KEY=
LARK_VERIFICATION_TOKEN=

# Feishu (domestic China version, independent app credentials)
FEISHU_APP_ID=
FEISHU_APP_SECRET=


# ==============================================================
# Analytics & Marketing
# ==============================================================

# PostHog (product analytics)
# https://posthog.com/
NEXT_PUBLIC_POSTHOG_KEY=
NEXT_PUBLIC_POSTHOG_HOST=https://us.i.posthog.com
# Server-side PostHog API key (for backend event ingestion)
POSTHOG_API_KEY=

# ==============================================================
# Ads — Frontend conversion tracking (all optional, disabled when empty)
# ==============================================================
# Google Ads: Get from Google Ads → Tools → Conversions → Tag setup
NEXT_PUBLIC_GOOGLE_ADS_MEASUREMENT_ID=
NEXT_PUBLIC_GOOGLE_ADS_CONVERSION_LABEL=
# Twitter/X Ads: Get from Twitter Ads → Events Manager
NEXT_PUBLIC_TWITTER_PIXEL_ID=
NEXT_PUBLIC_TWITTER_PURCHASE_EVENT_ID=
# PropellerAds: Get from PropellerAds dashboard
NEXT_PUBLIC_PROPELLER_AID=
NEXT_PUBLIC_PROPELLER_TID=
# TrafficJunky: Get from TrafficJunky dashboard
NEXT_PUBLIC_TJ_ACCOUNT_ID=
NEXT_PUBLIC_TJ_MEMBER_ID=

# ==============================================================
# Ads — Google Ads Server API (offline conversion upload, all optional)
# Docs: https://developers.google.com/google-ads/api/docs/get-started/introduction
# ==============================================================
GOOGLE_ADS_DEVELOPER_TOKEN=
GOOGLE_ADS_CLIENT_ID=
GOOGLE_ADS_CLIENT_SECRET=
GOOGLE_ADS_REFRESH_TOKEN=
GOOGLE_ADS_CUSTOMER_ID=
# Optional: MCC (manager account) ID
# GOOGLE_ADS_MCC_ID=
# Conversion action IDs from your Google Ads account
GOOGLE_ADS_CONVERSION_ACTION_ID=
GOOGLE_ADS_WEB_CONVERSION_ACTION_ID=


# ==============================================================
# Security & Anti-abuse
# ==============================================================

# Cloudflare Turnstile (CAPTCHA)
# https://developers.cloudflare.com/turnstile/
NEXT_PUBLIC_TURNSTILE_SITE_KEY=
TURNSTILE_SECRET_KEY=

# Cookie secure flag override
# - "true"  → cookies use Secure attribute (standard HTTPS)
# - "false" → disable Secure (required for Cloudflare Flexible SSL)
# - (empty) → auto: secure in production, insecure in development
COOKIE_SECURE=

# Anti-abuse thresholds. Defaults preserve the current signup policy.
# EMAIL_ABUSE_SAME_IP_WINDOW_HOURS=24
# EMAIL_ABUSE_SAME_IP_MIN_PRIOR_DIFFERENT_DEVICE=1
# EMAIL_ABUSE_SAME_IP_MIN_PRIOR_UNKNOWN_DEVICE=1
# EMAIL_ABUSE_SAME_IP_MAX_TOTAL_HISTORY=20

# Velobase Billing (credits management)
# https://velobase.io
VELOBASE_API_KEY=

# ==============================================================
# Tooling and infrastructure (not validated by src/env.js)
# These are consumed before application startup or by supporting libraries.
# ==============================================================

# Seed-only administrator account. Prisma seed defaults to admin@example.com.
# ADMIN_EMAIL=admin@example.com

# Docker entrypoint controls.
# SKIP_MIGRATION=true
# MIGRATION_OPTIONAL=true
# SKIP_SEED=true
# SEED_OPTIONAL=true

# Skip createEnv validation for image/build stages that inject config later.
# SKIP_ENV_VALIDATION=true

# Enable debug logs for compatible libraries, e.g. openai-agents:*
# DEBUG=

# Conventional proxy variables used by HTTP client libraries.
# HTTPS_PROXY=http://127.0.0.1:7897
# HTTP_PROXY=http://127.0.0.1:7897
# Platform shells use their own validated environment boundaries; Web ignores these.
# Export these in your shell when running platform commands (Desktop does not load .env).
# VELOBASE_DESKTOP_API_ORIGIN=https://your-app.example.com
# VELOBASE_MOBILE_API_ORIGIN=https://your-app.example.com
# VELOBASE_MOBILE_ENV=production

# Mailbox operation can be configured in Admin; off prevents mailbox workers from starting.
EMAIL_MANAGEMENT_MODE=auto
# Local Compose experience only; keep off in deployed environments.
NEXT_PUBLIC_LOCAL_DEMO=off
